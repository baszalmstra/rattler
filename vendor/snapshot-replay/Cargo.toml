[package]
name = "snapshot-replay"
version = "0.1.0"
edition = "2021"

[dependencies]
resolvo = { version = "=0.12.0", features = ["serde"] }
bincode = "1"
serde_json = "1"

[profile.release]
opt-level = 3

[workspace]

